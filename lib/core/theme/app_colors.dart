import 'package:flutter/material.dart';

class AppColors {
  // Color principal morado oscuro
  static const Color primary = Color(0xFF3D1A4D);
  static const Color primaryLight = Color(0xFF5A2968);
  static const Color primaryDark = Color(0xFF2A0E38);
  
  // Colores de fondo
  static const Color background = Color(0xFFF5F5F5);
  static const Color white = Color(0xFFFFFFFF);
  
  // Colores de texto
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textHint = Color(0xFF9E9E9E);
  static const Color textWhite = Color(0xFFFFFFFF);

  
  // Colores de estados
  static const Color error = Color(0xFFD32F2F);
  static const Color success = Color(0xFF388E3C);
  
  // Colores de botones sociales
  static const Color google = Color(0xFFFFFFFF);
  static const Color apple = Color(0xFF000000);
  static const Color bottom =  Colors.purpleAccent;
}
